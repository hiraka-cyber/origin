<%= form_for(current_user.followiwng_relationships.build(following_id: @user.id)) do |f| %>
<div class="">
  <%= f.hidden_field :following_id %>
</div>
<%= f.submit "Follow",class: "btn btn-primary follow-btn" %>
<% end %>
